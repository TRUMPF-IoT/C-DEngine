# C-DEngine Unit Tests
This project hosts all unit tests for the C-DEngine.


The NUnit Framework is used with the alias "MyNUnit" to prevent collisions between ConcurrentQueue in System.Threading and NUnit. 

